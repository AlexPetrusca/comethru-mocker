apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
spec:
  selector:
    app: {{ .Release.Name }}
  ports:
    - name: http
      port: 8090
      targetPort: 8090
    - name: h2
      port: 8092
      targetPort: 8092
